import axios from 'axios'

const state = {
    todos: []
}

const getters = {
    todos: (state) => state.todos
}

const actions = {

}

const mutations = {}

export default {
    state,
    getters,
    actions,
    mutations
}